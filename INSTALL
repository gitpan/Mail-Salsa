- Create salsa directory.

# cd /usr/local
# mkdir salsa
# chown -R mail:mail /usr/local/salsa

- Unpack the module and install it.

# tar xvfz Mail-Salsa-x.xx.tar.gz
# cd Mail-Salsa/tools
# perl Makefile.PL
# make
# make test
# make install 

# cp tools/fiesta /usr/local/bin
# cp tools/cucaracha /usr/local/bin
# cp tools/cucaracha.conf /etc/cucaracha.conf
# chmod 755 /usr/local/bin/cucaracha
# chmod 755 /usr/local/bin/fiesta

- Gentoo Linux

# cd /usr/adm/sm.bin
# ln -s /usr/local/bin/cucaracha cucaracha

- RedHat Linux

# cd /etc/smrsh
# ln -s /usr/local/bin/cucaracha cucaracha

- Edit cucaracha.conf

# nano cucaracha.conf

- Setup sendmail

# cd /etc/mail

# nano sendmail.mc

define(`ALIAS_FILE', `/etc/mail/aliases,/etc/mail/salsa.aliases')dnl

# touch /etc/mail/salsa.aliases
# m4 /etc/mail/sendmail.mc > /etc/mail/sendmail.cf

- Add aliases

# nano /etc/mail/aliases

salsa: you@yourdomain.org
salsa-master: you@yourdomain.org

# touch /etc/mail/salsa.aliases

# newaliases
# /etc/init.d/sendmail restart

- Add a mailling list to the system

# fiesta --help
# fiesta --add --list=mylist@mydomain.org --owner=hdias@aesbuc.pt

- To setup a list send a message to: mylist-admin@mydomain.org
